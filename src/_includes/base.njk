<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>{{ title or "KPITAL" }}</title>
{% include "head-seo.njk" %}
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Hanken+Grotesk:wght@500;600;700&family=Inter:wght@400;500&display=swap" rel="stylesheet">
<link rel="stylesheet" href="/assets/css/kpital.css">
{% if pageCss %}<link rel="stylesheet" href="/assets/css/pages/{{ pageCss }}.css">{% endif %}
</head>
<body{% if bodyClass %} class="{{ bodyClass }}"{% endif %}>
{% include "svgdefs.njk" %}
{% include "nav.njk" %}
{{ content | safe }}
{% if not hideFooter %}{% include "footer.njk" %}{% endif %}
<script src="/assets/js/app.js" defer></script>
</body>
</html>
